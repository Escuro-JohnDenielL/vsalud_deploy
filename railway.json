{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Docker",
    "watchPatterns": [
      "app/**",
      "config/**",
      "resources/**",
      "routes/**",
      "database/**",
      "composer.json",
      "composer.lock",
      "package.json",
      "Docker"
    ]
  },
  "deploy": {
    "startCommand": "sh -c \"php artisan migrate --force --path=database/migrations/2026_07_23_000001_create_incident_logs_table.php && php artisan serve --host=0.0.0.0 --port=$PORT\"",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
